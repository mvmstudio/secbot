# Security Monitor Bot

Telegram бот для мониторинга GitHub Dependabot Security Alerts. Отправляет ежедневные отчёты о найденных уязвимостях в ваших репозиториях.

## Возможности

- Мониторинг всех репозиториев GitHub-аккаунта
- Группировка уязвимостей по критичности (Critical, High, Moderate, Low)
- Ежедневная отправка отчётов в Telegram
- Ручная проверка по команде `/update`
- Простая система активации пользователей

---

## Концепция: Мета-репозитории

### Идея

Этот бот использует GitHub Dependabot как **базу знаний об уязвимостях**. Суть подхода:

1. Вы создаёте один или несколько **мета-репозиториев** на GitHub
2. В этих репозиториях вы перечисляете **все пакеты**, которые используете в своих проектах
3. GitHub Dependabot автоматически следит за уязвимостями в этих пакетах
4. Бот уведомляет вас в Telegram, когда появляются уязвимости

**По сути, GitHub становится вашей централизованной системой мониторинга зависимостей.**

### Создание мета-репозитория

#### Шаг 1: Создайте репозиторий

```bash
# Создайте приватный репозиторий на GitHub, например:
# myorg/security-watchlist или myorg/deps-monitor
```

#### Шаг 2: Добавьте файлы зависимостей

Создайте файлы с пакетами, за которыми хотите следить:

**Для JavaScript/Node.js проектов — `package.json`:**
```json
{
  "name": "security-watchlist",
  "private": true,
  "dependencies": {
    "next": "^14.0.0",
    "react": "^18.0.0",
    "axios": "^1.6.0",
    "express": "^4.18.0",
    "lodash": "^4.17.0"
  }
}
```

**Для Python проектов — `requirements.txt`:**
```
django>=4.2.0
flask>=3.0.0
requests>=2.31.0
aiohttp>=3.9.0
python-jose>=3.3.0
```

**Для PHP проектов — `composer.json`:**
```json
{
  "require": {
    "laravel/framework": "^10.0",
    "guzzlehttp/guzzle": "^7.0"
  }
}
```

#### Шаг 3: Включите Dependabot

1. GitHub → Repository → Settings → Security
2. Включите **Dependabot alerts**
3. Опционально: включите **Dependabot security updates** для автоматических PR

#### Шаг 4: Добавляйте новые пакеты

Каждый раз, когда начинаете использовать новый пакет в любом проекте — добавьте его в мета-репозиторий.

### Пример структуры мета-репозитория

```
security-watchlist/
├── package.json          # JavaScript/Node.js пакеты
├── requirements.txt      # Python пакеты
├── composer.json         # PHP пакеты
├── Gemfile              # Ruby пакеты
├── go.mod               # Go модули
└── README.md            # Описание что это за репо
```

### Важно: Ваша ответственность

> **Бот сообщает об уязвимостях — но обновлять пакеты в реальных проектах должны вы!**

Когда бот присылает уведомление:

1. **Проверьте**, какие ваши реальные проекты используют этот пакет
2. **Обновите** пакет в каждом из этих проектов:
   ```bash
   npm update package-name
   # или
   pip install --upgrade package-name
   ```
3. **Протестируйте** что ничего не сломалось
4. **Задеплойте** обновлённую версию

Мета-репозиторий — это только **система оповещения**. Обновление пакета в мета-репозитории НЕ обновляет его в ваших реальных проектах.

### Преимущества подхода

| Преимущество | Описание |
|--------------|----------|
| Централизация | Все пакеты всех проектов в одном месте |
| Единая точка мониторинга | Один бот следит за всем стеком |
| Не нужен доступ к рабочим репозиториям | Можно следить за пакетами без доступа к коду |
| Работает с любым стеком | JS, Python, PHP, Ruby, Go — всё в одном месте |
| Бесплатно | GitHub Dependabot бесплатен даже для приватных репозиториев |

---

## Быстрый старт

```bash
git clone git@github.com:mvmstudio/secbot.git
cd secbot
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
cp .env.example .env
# Заполнить .env (см. инструкции ниже)
python bot.py
```

---

## Подробная настройка

### Шаг 1: Создание Telegram бота

1. Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)

2. Отправьте команду `/newbot`

3. Введите **имя бота** (отображаемое имя):
   ```
   Security Monitor
   ```

4. Введите **username бота** (должен заканчиваться на `bot`):
   ```
   my_security_monitor_bot
   ```

5. BotFather выдаст токен вида:
   ```
   1234567890:AAHdqTcvCH1vGWJxfSeofSAs0K5PALDsaw
   ```

6. Скопируйте этот токен — это ваш `TELEGRAM_BOT_TOKEN`

**Опционально:** настройте бота через BotFather:
- `/setdescription` — описание бота
- `/setabouttext` — информация в профиле
- `/setuserpic` — аватарка бота

---

### Шаг 2: Создание GitHub Token

GitHub токен нужен для доступа к Dependabot Security Alerts ваших репозиториев.

1. Перейдите в [GitHub Settings → Developer settings → Personal access tokens → Tokens (classic)](https://github.com/settings/tokens)

2. Нажмите **"Generate new token"** → **"Generate new token (classic)"**

3. Заполните форму:
   - **Note:** `Security Monitor Bot` (любое название для себя)
   - **Expiration:** выберите срок действия (рекомендуем 90 days, потом обновить)

4. Выберите **scopes** (права доступа):

   | Scope | Зачем нужен |
   |-------|-------------|
   | `repo` | Доступ к приватным репозиториям |
   | `security_events` | Чтение Dependabot alerts |

   Минимально нужно отметить:
   - [x] `repo` (полный доступ к репозиториям)
   - [x] `security_events` (чтение security alerts)

5. Нажмите **"Generate token"**

6. **ВАЖНО:** Скопируйте токен сразу! GitHub покажет его только один раз:
   ```
   ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
   ```

7. Это ваш `GITHUB_TOKEN`

---

### Шаг 3: Генерация ACTIVATION_TOKEN

`ACTIVATION_TOKEN` — это секретный токен для активации пользователей бота. Только те, кто знает этот токен, смогут пользоваться ботом.

**Зачем это нужно:**
- Защита от спама и несанкционированного использования
- Контроль над тем, кто получает уведомления
- Можно поделиться токеном только с членами команды

**Генерация токена:**

```bash
python -c "import uuid; print(uuid.uuid4())"
```

Результат будет примерно таким:
```
a1b2c3d4-e5f6-7890-abcd-ef1234567890
```

Это ваш `ACTIVATION_TOKEN`. Сохраните его — вы будете отправлять его пользователям для активации.

---

### Шаг 4: Настройка .env файла

1. Скопируйте пример:
   ```bash
   cp .env.example .env
   ```

2. Откройте `.env` в редакторе и заполните:

   ```env
   # Telegram Bot
   TELEGRAM_BOT_TOKEN=1234567890:AAHdqTcvCH1vGWJxfSeofSAs0K5PALDsaw

   # GitHub Personal Access Token (needs repo and security_events scope)
   GITHUB_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

   # Activation Token (generate with: python -c "import uuid; print(uuid.uuid4())")
   ACTIVATION_TOKEN=a1b2c3d4-e5f6-7890-abcd-ef1234567890
   ```

3. Убедитесь, что `.env` добавлен в `.gitignore` (уже добавлен по умолчанию)

---

## Запуск

### Интерактивный режим (бот)

```bash
source venv/bin/activate
python bot.py
```

Бот запустится и будет ждать сообщений. Для остановки: `Ctrl+C`

### Запуск в фоне (production)

**Вариант 1: systemd (рекомендуется для Linux)**

Создайте файл `/etc/systemd/system/secbot.service`:

```ini
[Unit]
Description=Security Monitor Telegram Bot
After=network.target

[Service]
Type=simple
User=www-data
WorkingDirectory=/opt/secbot
ExecStart=/opt/secbot/venv/bin/python bot.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

Команды:
```bash
sudo systemctl daemon-reload
sudo systemctl enable secbot
sudo systemctl start secbot
sudo systemctl status secbot
```

**Вариант 2: screen/tmux**

```bash
screen -S secbot
source venv/bin/activate
python bot.py
# Ctrl+A, D — отключиться от сессии
# screen -r secbot — вернуться
```

### Ежедневные отчёты (cron)

Скрипт `monitor.py` отправляет отчёт всем активированным пользователям. Настройте cron для ежедневного запуска:

```bash
crontab -e
```

Добавьте строку (отчёт в 9:00 каждый день):

```cron
0 9 * * * cd /opt/secbot && /opt/secbot/venv/bin/python monitor.py >> /var/log/secbot.log 2>&1
```

---

## Использование бота

### Для администратора

1. Запустите бота
2. Отправьте `ACTIVATION_TOKEN` членам команды (лично, в приватном чате)

### Для пользователя

1. Найдите бота в Telegram по username
2. Нажмите **Start** или отправьте `/start`
3. Активируйтесь командой:
   ```
   /activate a1b2c3d4-e5f6-7890-abcd-ef1234567890
   ```
4. После активации доступны команды:
   - `/status` — проверить статус активации
   - `/update` — получить отчёт о уязвимостях прямо сейчас

---

## Команды бота

| Команда | Описание | Требует активации |
|---------|----------|-------------------|
| `/start` | Приветствие и инструкция | Нет |
| `/activate <token>` | Активация пользователя | Нет |
| `/status` | Статус активации | Да |
| `/update` | Проверить уязвимости | Да |

---

## Структура проекта

```
secbot/
├── bot.py              # Telegram бот (основной процесс)
├── monitor.py          # Скрипт для cron (ежедневные отчёты)
├── github_client.py    # Клиент GitHub API
├── storage.py          # Хранилище активированных пользователей (JSON)
├── requirements.txt    # Python зависимости
├── .env.example        # Пример переменных окружения
├── .env                # Ваши секреты (не в git!)
├── activated_users.json # Данные пользователей (создаётся автоматически)
└── README.md
```

---

## Пример отчёта

```
🛡️ Security Monitor Report
📅 Найдено уязвимостей: 5

🔴 CRITICAL (2)
─────────────────────────
📦 next
   📁 myorg/myproject
   🆔 CVE-2025-12345
   ⬆️ Обновить до: 14.2.1
   🔗 Подробнее

🟠 HIGH (3)
─────────────────────────
📦 axios
   📁 myorg/api-service
   🆔 GHSA-abcd-1234-efgh
   ⬆️ Обновить до: 1.6.0
   🔗 Подробнее

💡 Рекомендация: Обновите зависимости командой npm update или pip install --upgrade
```

---

## Troubleshooting

### Бот не отвечает

1. Проверьте, что `TELEGRAM_BOT_TOKEN` корректный
2. Убедитесь, что бот запущен (`python bot.py`)
3. Проверьте логи на ошибки

### Нет уязвимостей в отчёте

1. Убедитесь, что `GITHUB_TOKEN` имеет права `repo` и `security_events`
2. Проверьте, что Dependabot включен в репозиториях:
   - GitHub → Repository → Settings → Security → Dependabot alerts → Enable

### Ошибка 403/404 при получении alerts

- 403: Dependabot alerts отключены для репозитория
- 404: Нет доступа к репозиторию (проверьте права токена)

### Токен GitHub истёк

GitHub токены имеют срок действия. Создайте новый токен и обновите `.env`.

---

## Безопасность

- **Никогда** не коммитьте `.env` в git
- Храните `ACTIVATION_TOKEN` в секрете — делитесь только с доверенными людьми
- Регулярно обновляйте `GITHUB_TOKEN` (рекомендуем каждые 90 дней)
- Файл `activated_users.json` содержит chat_id пользователей — не публикуйте его

---

## Лицензия

MIT
