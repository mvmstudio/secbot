# Security Monitor Bot

Telegram бот для мониторинга GitHub Dependabot Security Alerts. Отправляет ежедневные отчёты о найденных уязвимостях в ваших репозиториях.

## Возможности

- Мониторинг всех репозиториев GitHub-аккаунта
- Группировка уязвимостей по критичности (Critical, High, Moderate, Low)
- Ежедневная отправка отчётов в Telegram
- Ручная проверка по команде `/update`
- Простая система активации пользователей

## Установка

### 1. Клонирование

```bash
git clone <repo-url>
cd secbot
```

### 2. Создание виртуального окружения

```bash
python3 -m venv venv
source venv/bin/activate  # Linux/macOS
# или
venv\Scripts\activate     # Windows
```

### 3. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 4. Настройка переменных окружения

```bash
cp .env.example .env
```

Заполните `.env`:

| Переменная | Описание |
|------------|----------|
| `TELEGRAM_BOT_TOKEN` | Токен Telegram бота (получить у [@BotFather](https://t.me/BotFather)) |
| `GITHUB_TOKEN` | GitHub Personal Access Token с правами `repo` и `security_events` |
| `ACTIVATION_TOKEN` | Токен для активации пользователей (любой UUID) |

**Генерация ACTIVATION_TOKEN:**
```bash
python -c "import uuid; print(uuid.uuid4())"
```

**Создание GitHub Token:**
1. GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
2. Generate new token
3. Выбрать scopes: `repo`, `security_events`

## Запуск

### Интерактивный режим (бот)

```bash
python bot.py
```

### Отправка отчёта (для cron)

```bash
python monitor.py
```

### Настройка cron (ежедневно в 9:00)

```bash
crontab -e
```

Добавить:
```
0 9 * * * cd /path/to/secbot && /path/to/venv/bin/python monitor.py
```

## Команды бота

| Команда | Описание |
|---------|----------|
| `/start` | Приветствие и инструкция |
| `/activate <token>` | Активация пользователя |
| `/status` | Статус активации |
| `/update` | Принудительная проверка уязвимостей |

## Структура проекта

```
secbot/
├── bot.py              # Telegram бот
├── monitor.py          # Скрипт для cron (ежедневные отчёты)
├── github_client.py    # Клиент GitHub API
├── storage.py          # Хранилище активированных пользователей
├── requirements.txt    # Зависимости
├── .env.example        # Пример переменных окружения
└── README.md
```

## Пример отчёта

```
🛡️ Security Monitor Report
📅 Найдено уязвимостей: 5

🔴 CRITICAL (2)
─────────────────────────
📦 next
   📁 myorg/myproject
   🆔 CVE-2025-12345
   ⬆️ Обновить до: 14.2.1
   🔗 Подробнее

🟠 HIGH (3)
─────────────────────────
...
```

## Лицензия

MIT
